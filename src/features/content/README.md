# Content feature (Phase 2)

Will host:
- Editorial list views: `/aprende`, `/peptidos/[slug]`, `/blog`
- Article schema + FAQ schema + Breadcrumb schema
- `content_items` query layer (Server-first; reads via anon client)
- Draft → in_review → published workflow tools

Build only after approval of Phase 1.
