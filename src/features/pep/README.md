# Pep feature (Phase 2)

Will host:
- Server adapter for the Pep AI backend (via secure API key)
- Rate-limited chat endpoint (`/api/pep`)
- Streaming conversation client component
- Conversation persistence (Supabase table added in Phase 2)

Build only after approval of Phase 1.
