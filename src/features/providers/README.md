# Providers feature (Phase 2)

Will host:
- Provider data model (already shaped in `src/types/supabase.ts`)
- Server actions that build `referral_url` signed with `AFFILIATE_SIGNING_SECRET`
- `/donde-comprar` listing UI
- `/recursos/[region]` regional pages — only published with verified content

Build only after approval of Phase 1.
