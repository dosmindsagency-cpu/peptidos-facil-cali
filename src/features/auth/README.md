# Auth feature (Phase 2)

Will host:
- Login / signup routes (`/login`, `/signup`)
- Auth callback (`/auth/callback`)
- Server actions for `signInWithPassword`, `signOut`
- Hooks for client auth state
- Middleware that protects private routes

Build only after approval of Phase 1.
